# Etsy API采集说明

## Etsy API到底采集了什么？

### 核心逻辑

```python
# 我的脚本做的事：
1. 用你的"主关键词"（如："smart pet feeder"）
2. 调用Etsy搜索API
3. 返回前20个搜索结果的商品

相当于：
在Etsy网站搜索框输入 "smart pet feeder"
→ 获取搜索结果页面前20个商品
```

---

## 重要问题：这些一定是你的竞品吗？

### ❌ 不一定！

Etsy搜索结果可能包含：

#### 1. ✅ 真正的竞品
```
示例：搜索 "smart pet feeder"
返回：
- Smart Pet Feeder with WiFi (真正竞品)
- Automatic Dog Feeder (真正竞品)
- Pet Food Dispenser (真正竞品)
```

#### 2. ⚠️ 相关但不同类的产品
```
示例：搜索 "smart pet feeder"
可能返回：
- Pet Feeder Stand (只是架子，不是喂食器)
- Pet Food Storage (食物储存罐，不是喂食器)
- Pet Bowl Set (碗，不是自动喂食器)
```

#### 3. ❌ 不相关的产品
```
示例：搜索 "handmade wallet"
可能返回：
- Handmade Leather Bag (有"handmade"但不是wallet)
- Wallet Chain (配件，不是主产品)
- Vintage Purse (标签包含wallet但是女式钱包)
```

---

## 为什么会这样？

### Etsy搜索算法

```
Etsy搜索是基于：
1. 标题匹配（Title）
2. 标签匹配（Tags）
3. 类目相关性
4. 店铺质量评分
5. 销售历史

→ 只要商品的标题/标签包含你的关键词，就可能出现在结果中
→ 不保证是完全相同的产品类型
```

---

## 那Etsy API的数据有什么用？

### 虽然不是100%精确竞品，但仍然非常有价值：

#### 1. 市场价格参考 ✅
```
即使混入了一些不完全相同的产品：
- 平均价格仍然能反映市场水平
- 价格区间能帮助定价决策
```

#### 2. 市场需求指标 ✅
```
收藏数、浏览数反映：
- 这个关键词下的产品有多受欢迎
- 市场整体需求水平
```

#### 3. SEO/标签研究 ✅
```
查看返回商品使用的标签：
- 发现相关关键词
- 优化自己的listing
- 了解竞品如何描述产品
```

#### 4. 竞争程度 ✅
```
返回商品数量反映：
- 市场饱和度
- 竞争激烈程度
```

#### 5. 销量估算（粗略）⚠️
```
通过收藏数估算销量：
- 给出市场潜力的大致范围
- 不是精确数字，但足够做决策
```

---

## 如何判断是不是真正的竞品？

### 方法1: 人工筛选（推荐）

```python
脚本采集完数据后：
1. 查看返回的商品标题
2. 手动判断是否是真正竞品
3. 剔除不相关的
4. 重新计算统计数据
```

### 方法2: 多关键词交叉验证

```
用多个相关关键词搜索：
- "smart pet feeder"
- "automatic pet feeder"
- "wifi pet feeder"

如果同一个商品在多个关键词下都出现：
→ 更可能是真正竞品
```

### 方法3: 价格区间过滤

```
如果你的产品定价 $40-50：
- 过滤掉 < $20 的商品（可能是配件）
- 过滤掉 > $100 的商品（可能是高端产品）
```

---

## 改进：我可以加过滤功能

让我展示一个改进版本，加入过滤选项：

```python
def fetch_etsy_competitors(
    keyword: str,
    limit: int = 20,
    min_price: float = 0,      # 最低价格过滤
    max_price: float = 999999, # 最高价格过滤
    exclude_keywords: List[str] = []  # 排除包含这些词的商品
):
    """
    采集Etsy竞品，带过滤功能

    参数：
    - min_price: 过滤低于此价格的商品
    - max_price: 过滤高于此价格的商品
    - exclude_keywords: 排除标题包含这些词的商品
      例如：['stand', 'bowl', 'accessory']
    """
    # 获取搜索结果
    results = call_etsy_api(keyword, limit)

    # 过滤结果
    filtered = []
    for item in results:
        # 价格过滤
        price = item['price']['amount'] / 100
        if price < min_price or price > max_price:
            continue

        # 关键词排除
        title = item['title'].lower()
        if any(exclude in title for exclude in exclude_keywords):
            continue

        filtered.append(item)

    return filtered
```

---

## 实际使用建议

### 阶段1: 初步验证（使用API）

```
目的：快速了解市场概况
- 不要太在意是否100%是竞品
- 关注整体趋势和数据范围
- 快速决策：GO / NO-GO
```

### 阶段2: 深度研究（手动验证）

```
如果初步验证通过，再手动：
1. 访问Etsy网站
2. 搜索相同关键词
3. 人眼筛选真正的前10-20个竞品
4. 详细分析每个竞品的：
   - 产品功能
   - 图片质量
   - 描述文案
   - 评论内容
   - 定价策略
```

---

## 对比：Etsy API vs 手动研究

| 方面 | Etsy API（自动） | 手动研究 |
|------|----------------|---------|
| **速度** | ⚡ 秒级 | 🐢 小时级 |
| **准确度** | ⚠️  70-80% | ✅ 100% |
| **数据量** | ✅ 可采集数百个 | ❌ 最多10-20个 |
| **适用阶段** | 初步筛选 | 深度验证 |
| **成本** | 免费 | 时间成本 |

---

## 我的推荐工作流

```
Step 1: eRank关键词研究
→ 了解搜索量和趋势

Step 2: Etsy API自动采集（我的脚本）
→ 快速获取市场概况
→ 价格区间、收藏数、竞争程度

Step 3: 初步决策
→ 如果数据不理想，直接放弃
→ 如果数据理想，继续深入

Step 4: 手动验证（如果通过Step 3）
→ 访问Etsy网站
→ 人工筛选真正的竞品
→ 详细分析前10个

Step 5: 最终决策
→ GO: 开始开发产品
→ NO-GO: 换下一个产品
```

---

## 总结

### Etsy API采集的是什么？

```
✅ 是：基于关键词的搜索结果
✅ 包含：价格、收藏、浏览、标签等数据
⚠️  不是：100%精确的竞品列表
⚠️  可能：混入相关但不完全相同的产品

但对于产品验证：
✅ 足够做初步判断
✅ 大大节省时间
✅ 提供量化决策依据
```

### 如何最大化价值？

```
1. 用API做初步筛选（快）
2. 用手动做深度验证（准）
3. 结合两者优势
4. 不要依赖单一数据源
```

### 什么时候API数据最准确？

```
✅ 产品类目明确（如"pet feeder"）
✅ 关键词专业（如"leather wallet"）
✅ 市场成熟（竞品多）

⚠️  准确度较低的情况：
❌ 关键词太宽泛（如"gift"）
❌ 多义词（如"charm"可以是饰品或魅力）
❌ 新兴产品（竞品少）
```
